check-sources=true
external-sources=true

# Enable checks disabled by default

#enable=require-variable-braces         # don't make scripts into perl
#enable=check-extra-masked-returns      # unnecessary with "set -Ee"
#enable=check-set-e-suppressed          # unnecessary with "set -Ee"

enable=add-default-case
enable=avoid-negated-conditions
enable=avoid-nullary-conditions
enable=check-unassigned-uppercase
enable=deprecate-which
enable=quote-safe-variables
enable=require-double-brackets
enable=useless-use-of-cat
