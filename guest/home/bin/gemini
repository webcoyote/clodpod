#!/usr/bin/env bash
set -Eeuo pipefail

APP="$HOME/node_modules/bin/gemini"
if [[ ! -x "$APP" ]]; then
    echo >&2 "Installing gemini..."
    npm install --global --silent @google/gemini-cli
fi
exec "$APP" --yolo "$@"
